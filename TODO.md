# FastANSI TODOs

## Features
- [ ] **UI Character Constants (`FastUIChars` / `FastGeometrics`)**
  - Add a dedicated class containing static constants for common TUI/Terminal characters.
  - Progress bar blocks: `BLOCK_FULL = '\u2588'` (█), `BLOCK_SHADE_LIGHT = '\u2591'` (░), etc.
  - Box drawing characters (lines, corners, borders).
  - Goal: Allow developers to build rich console UIs without needing to memorize or hardcode Unicode escape sequences.

## Performance
- [ ] 

## Fixes
- [ ] 
